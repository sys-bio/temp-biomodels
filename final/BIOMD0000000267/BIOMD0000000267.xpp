
#
# This file is automatically generated with 
# the System Biology Format Converter (http://sbfc.sourceforge.net/)
# from an SBML file.
#

#
# Model name = Lebeda2008 - BoNT paralysis (3 step model)
#
# is http://identifiers.org/biomodels.db/MODEL1009070000
# is http://identifiers.org/biomodels.db/BIOMD0000000267
# isDescribedBy http://identifiers.org/pubmed/18551355
# isDerivedFrom http://identifiers.org/pubmed/6243359
#

# some function definitions that are allowed in SBML but not valid in xpp
ceil(x)=flr(1+x)

@delay=50


# Compartment: id = extracellular, name = extracellular, constant
par extracell=1.0

# Compartment: id = endosome, name = endosome, constant
par endosome=1.0

# Compartment: id = neuroplasm, name = neuroplasm, constant
par neuroplas=1.0

# Parameter:   id =  tension, name = tension, defined by a Rule

# assignmentRule: variable = tension
tension=1-lytic
aux tension=tension

# Reaction: id = endocytosis
# Local Parameter:   id =  kT, name = kT
par kT=0.141


endocytos=kT*bound*extracell

# Reaction: id = translocation
# Local Parameter:   id =  kL, name = kL
par kL=0.013


translo_1=kL*transloca*endosome

# Reaction: id = binding
# Local Parameter:   id =  kB, name = kB
par kB=0.058


binding=kB*free*extracell

# Species:   id = free, name = free_BoNT/A, affected by kineticLaw

init free=1.0
dfree/dt=(1/(extracell))*((-1.0 * binding))

# Species:   id = bound, name = bound_BoNT/A, affected by kineticLaw

init bound=0.0
dbound/dt=(1/(extracell))*((-1.0 * endocytos) + ( 1.0 * binding))

# Species:   id = translocate, name = transloc_BoNT/A, affected by kineticLaw

par transloca=0.0
aux transloca=transloca
dtransloca/dt=(1/(endosome))*(( 1.0 * endocytos) + (-1.0 * translo_1))

# Species:   id = lytic, name = lytic_BoNT/A, affected by kineticLaw

init lytic=0.0
dlytic/dt=(1/(neuroplas))*(( 1.0 * translo_1))

@ meth=cvode, tol=1e-6, atol=1e-8
# @ maxstor=1e6
@ bound=40000, total=200
done

