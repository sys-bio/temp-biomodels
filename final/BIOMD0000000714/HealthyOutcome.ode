
#
# This file is automatically generated with 
# the System Biology Format Converter (http://sbfc.sourceforge.net/)
# from an SBML file.
#

#
# Model name = Reynolds2006 - Reduced model of the acute inflammatory response
#
# is http://identifiers.org/biomodels.db/MODEL1808280003
# is http://identifiers.org/biomodels.db/BIOMD0000000714
#

# some function definitions that are allowed in SBML but not valid in xpp
ceil(x)=flr(1+x)

@delay=50


# FunctionDefinition id = Constant_flux__irreversible, name = Constant flux (irreversible)
Constant_(v)=(v)


# FunctionDefinition id = Function_for_Reaction_1, name = Function for Reaction 1 [1]
Function_(k_pg,P,p_inf)=(k_pg*P*(1-P/p_inf))


# FunctionDefinition id = Function_for_Reaction_2, name = Function for Reaction 2
Functio_1(k_pm,s_m,P,mu_m,k_mp)=(k_pm*s_m*P/(mu_m+k_mp*P))


# FunctionDefinition id = Function_for_Reaction_3, name = Function for Reaction 3
Functio_2(f_Nstar,P,k_pn)=(k_pn*f_Nstar*P)


# FunctionDefinition id = Function_for_Reaction_4, name = Function for Reaction 4
Functio_3(s_nr,R,mu_nr)=(s_nr*R/(mu_nr+R))


# FunctionDefinition id = Function_for_Reaction_6, name = Function for Reaction 6
Functio_4(k_dn,f_s_f_Nstar)=(k_dn*f_s_f_Nstar)


# FunctionDefinition id = Function_for_Reaction_9, name = Function for Reaction 9
Functio_5(k_cn,f_Nstar_k_cnd_D)=(k_cn*f_Nstar_k_cnd_D/(1+f_Nstar_k_cnd_D))


# Compartment: id = compartment, name = Human body, constant
par compartme=1.0

# Parameter:   id =  k_pm, name = k_pm, constant
par k_pm=0.6

# Parameter:   id =  k_mp, name = k_mp, constant
par k_mp=0.01

# Parameter:   id =  s_m, name = s_m, constant
par s_m=0.005

# Parameter:   id =  mu_m, name = mu_m, constant
par mu_m=0.002

# Parameter:   id =  k_pg, name = k_pg, constant
par k_pg=0.3

# Parameter:   id =  p_inf, name = p_inf, constant
par p_inf=20.0

# Parameter:   id =  k_pn, name = k_pn, constant
par k_pn=1.8

# Parameter:   id =  k_np, name = k_np, constant
par k_np=0.1

# Parameter:   id =  k_nn, name = k_nn, constant
par k_nn=0.01

# Parameter:   id =  s_nr, name = s_nr, constant
par s_nr=0.08

# Parameter:   id =  mu_nr, name = mu_nr, constant
par mu_nr=0.12

# Parameter:   id =  mu_n, name = mu_n, constant
par mu_n=0.05

# Parameter:   id =  k_nd, name = k_nd, constant
par k_nd=0.02

# Parameter:   id =  k_dn, name = k_dn, constant
par k_dn=0.35

# Parameter:   id =  x_dn, name = x_dn, constant
par x_dn=0.06

# Parameter:   id =  mu_d, name = mu_d, constant
par mu_d=0.02

# Parameter:   id =  c_inf, name = c_inf, constant
par c_inf=0.28

# Parameter:   id =  s_c, name = s_c, constant
par s_c=0.0125

# Parameter:   id =  k_cn, name = k_cn, constant
par k_cn=0.04

# Parameter:   id =  k_cnd, name = k_cnd, constant
par k_cnd=48.0

# Parameter:   id =  mu_c, name = mu_c, constant
par mu_c=0.1

# Parameter:   id =  f_Nstar, name = f_Nstar, defined by a Rule

# Parameter:   id =  f_s_f_Nstar, name = f_s_f_Nstar, defined by a Rule

# Parameter:   id =  R, name = R, defined by a Rule

# Parameter:   id =  f_Nstar__k_cnd_D, name = f_Nstar_ k_cnd_D, defined by a Rule

# assignmentRule: variable = f_Nstar
f_Nstar=Nstar/(1+(C_A/c_inf)^2)
aux f_Nstar=f_Nstar

# assignmentRule: variable = f_s_f_Nstar
f_s_f_Nst=f_Nstar^6/(x_dn^6+f_Nstar^6)
aux f_s_f_Nst=f_s_f_Nst

# assignmentRule: variable = R
R=(k_nn*Nstar+k_np*P+k_nd*D)/(1+(C_A/c_inf)^2)
aux R=R

# assignmentRule: variable = f_Nstar__k_cnd_D
f_Nstar__=(Nstar+k_cnd*D)/(1+(C_A/c_inf)^2)
aux f_Nstar__=f_Nstar__

# Reaction: id = reaction_1, name = Production of Pathogen

reaction_=compartme*Function_(k_pg, P, p_inf)

# Reaction: id = reaction_2, name = Degradation of pathogen by non-specific local response

reactio_1=compartme*Functio_1(k_pm, s_m, P, mu_m, k_mp)

# Reaction: id = reaction_3, name = Degradation of pathogen by activated phagocytes

reactio_2=compartme*Functio_2(f_Nstar, P, k_pn)

# Reaction: id = reaction_4, name = Recruitment of Activated Phagocytes from the source

reactio_3=compartme*Functio_3(s_nr, R, mu_nr)

# Reaction: id = reaction_5, name = Death of activated phagocytes

reactio_4=compartme*mu_n*Nstar

# Reaction: id = reaction_6, name = Tissue damage produced by activated phagocytes

reactio_5=compartme*Functio_4(k_dn, f_s_f_Nst)

# Reaction: id = reaction_7, name = Removal of damaged tissue

reactio_6=compartme*mu_d*D

# Reaction: id = reaction_8, name = Recruitment of Anti-inflammatory mediators from the source

reactio_7=compartme*Constant_(s_c)

# Reaction: id = reaction_9, name = Production of anti-inflammatory mediators

reactio_8=compartme*Functio_5(k_cn, f_Nstar__)

# Reaction: id = reaction_10, name = Removal of anti-inflammatory mediators

reactio_9=compartme*mu_c*C_A

# Species:   id = P, name = Pathogen (P), affected by kineticLaw

init P=1.0
dP/dt=(1/(compartme))*(( 1.0 * reaction_) + (-1.0 * reactio_1) + (-1.0 * reactio_2))

# Species:   id = Nstar, name = Activated Phagocytes (Nstar), affected by kineticLaw

init Nstar=0.0
dNstar/dt=(1/(compartme))*(( 1.0 * reactio_3) + (-1.0 * reactio_4))

# Species:   id = D, name = Tissue Damage (D), affected by kineticLaw

init D=0.0
dD/dt=(1/(compartme))*(( 1.0 * reactio_5) + (-1.0 * reactio_6))

# Species:   id = C_A, name = Anti-inflammatory mediators (C_A), affected by kineticLaw

init C_A=0.125
dC_A/dt=(1/(compartme))*(( 1.0 * reactio_7) + ( 1.0 * reactio_8) + (-1.0 * reactio_9))

@ meth=cvode, tol=1e-6, atol=1e-8
# @ maxstor=1e6
@ bound=40000, total=200
done

