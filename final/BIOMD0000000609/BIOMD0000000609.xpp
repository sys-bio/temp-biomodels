
#
# This file is automatically generated with 
# the System Biology Format Converter (http://sbfc.sourceforge.net/)
# from an SBML file.
#

#
# Model name = Reddyhoff2015 - Acetaminophen metabolism and toxicity
#
# is http://identifiers.org/biomodels.db/MODEL1603080000
# is http://identifiers.org/biomodels.db/BIOMD0000000609
# isDescribedBy http://identifiers.org/pubmed/26348886
# isDerivedFrom http://identifiers.org/pubmed/26966512
#

# some function definitions that are allowed in SBML but not valid in xpp
ceil(x)=flr(1+x)

@delay=50


# FunctionDefinition id = Constant_flux__irreversible, name = Constant flux (irreversible)
Constant_(v)=(v)


# Compartment: id = compartment, name = compartment, constant
par compartme=1.0

# Parameter:   id =  P0, name = P0, constant
par P0=1.32E-13

# Parameter:   id =  dG, name = dG, constant
par dG=2.0

# Parameter:   id =  bG, name = bG, constant
par bG=1.374E-14

# Parameter:   id =  kGSH, name = kGSH, constant
par kGSH=1.6E18

# Parameter:   id =  kG, name = kG, constant
par kG=2.99

# Parameter:   id =  kS, name = kS, constant
par kS=2.26E14

# Parameter:   id =  bS, name = bS, constant
par bS=2.65E-14

# Parameter:   id =  dS, name = dS, constant
par dS=2.0

# Parameter:   id =  k450, name = k450, constant
par k450=0.315

# Parameter:   id =  kN, name = kN, constant
par kN=0.0315

# Parameter:   id =  kPSH, name = kPSH, constant
par kPSH=110.0

# Reaction: id = reaction_1, name = R1

reaction_=compartme*k450*Paracetam

# Reaction: id = R2, name = R2

R2=compartme*kN*NAPQI

# Reaction: id = R3, name = R3

R3=compartme*kG*Paracetam

# Reaction: id = R4, name = R4

R4=compartme*kPSH*NAPQI

# Reaction: id = R5, name = R5

R5=compartme*kGSH*NAPQI*GSH

# Reaction: id = R6, name = R6

R6=compartme*dG*GSH

# Reaction: id = R7, name = R7

R7=compartme*Constant_(bG)

# Reaction: id = R8, name = R8

R8=compartme*kS*Paracetam*Sulphate_

# Reaction: id = R9, name = R9

R9=compartme*dS*Sulphate_

# Reaction: id = R10, name = R10

R10=compartme*Constant_(bS)

# Species:   id = Sulphate__PAPS, name = Sulphate_PAPS, affected by kineticLaw

par Sulphate_=1.325E-14
aux Sulphate_=Sulphate_
dSulphate_/dt=(1/(compartme))*((-1.0 * R8) + (-1.0 * R9) + ( 1.0 * R10))

# Species:   id = GSH, name = GSH, affected by kineticLaw

init GSH=6.87E-15
dGSH/dt=(1/(compartme))*((-1.0 * R5) + (-1.0 * R6) + ( 1.0 * R7))

# Species:   id = NAPQI, name = NAPQI, affected by kineticLaw

init NAPQI=0.0
dNAPQI/dt=(1/(compartme))*(( 1.0 * reaction_) + (-1.0 * R2) + (-1.0 * R4) + (-1.0 * R5))

# Species:   id = Paracetamol_APAP, name = Paracetamol_APAP, affected by kineticLaw

par Paracetam=1.32E-13
aux Paracetam=Paracetam
dParacetam/dt=(1/(compartme))*((-1.0 * reaction_) + ( 1.0 * R2) + (-1.0 * R3) + (-1.0 * R8))

# Species:   id = Protein_adducts, name = Protein_adducts, affected by kineticLaw

par Protein_a=0.0
aux Protein_a=Protein_a
dProtein_a/dt=(1/(compartme))*(( 1.0 * R4))

@ meth=cvode, tol=1e-6, atol=1e-8
# @ maxstor=1e6
@ bound=40000, total=200
done

