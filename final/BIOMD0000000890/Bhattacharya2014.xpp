
#
# This file is automatically generated with 
# the System Biology Format Converter (http://sbfc.sourceforge.net/)
# from an SBML file.
#

#
# Model name = Bhattacharya2014 - A mathematical model of the sterol regulatory element binding protein 2 cholesterol biosynthesis pathway
#
# isDescribedBy http://identifiers.org/pubmed/24444765
# is http://identifiers.org/biomodels.db/MODEL1912120004
# is http://identifiers.org/biomodels.db/BIOMD0000000890
#

# some function definitions that are allowed in SBML but not valid in xpp
ceil(x)=flr(1+x)

@delay=50


# FunctionDefinition id = Function_for_HMGCR_Translation, name = Function_for_HMGCR_Translation
Function_(m)=(m)


# FunctionDefinition id = Function_for_HMGCR_Transcription, name = Function_for_HMGCR_Transcription
Functio_1(mu_m,kappa_m,c,kappa_c,y,x)=(mu_m/(1+(kappa_m*(1+(c/kappa_c)^y))^x))


# FunctionDefinition id = Function_for_Cholesterol_Synthesis, name = Function_for_Cholesterol_Synthesis
Functio_2(mu_c,h)=(mu_c*h)


# Compartment: id = compartment, name = compartment, constant
par compartme=1.0

# Parameter:   id =  mu_c, name = mu_c, constant
par mu_c=0.462

# Parameter:   id =  mu_m, name = mu_m, constant
par mu_m=1.9E-10

# Parameter:   id =  delta_m, name = delta_m, constant
par delta_m=0.00135

# Parameter:   id =  delta_h, name = delta_h, constant
par delta_h=0.00193

# Parameter:   id =  delta_c, name = delta_c, constant
par delta_c=0.0036

# Parameter:   id =  kappa_m, name = kappa_m, constant
par kappa_m=1.0E-4

# Parameter:   id =  kappa_c, name = kappa_c, constant
par kappa_c=0.001

# Parameter:   id =  x, name = x, constant
par x=3.0

# Parameter:   id =  y, name = y, constant
par y=4.0

# Parameter:   id =  HMGCR_Rescaled, name = HMGCR_Rescaled, defined by a Rule

# Parameter:   id =  mRNA_Rescaled, name = mRNA_Rescaled, defined by a Rule

# assignmentRule: variable = HMGCR_Rescaled
HMGCR_Res=h/0.001
aux HMGCR_Res=HMGCR_Res

# assignmentRule: variable = mRNA_Rescaled
mRNA_Resc=m/2E-6
aux mRNA_Resc=mRNA_Resc

# Reaction: id = HMGCR_Transcription, name = HMGCR_Transcription

HMGCR_Tra=compartme*Functio_1(mu_m, kappa_m, c, kappa_c, y, x)

# Reaction: id = HMGCR_mRNA_Degradation, name = HMGCR_mRNA_Degradation

HMGCR_mRN=compartme*delta_m*m

# Reaction: id = HMGCR_Translation, name = HMGCR_Translation

HMGCR_T_1=compartme*Function_(m)

# Reaction: id = HMGCR_Degradation, name = HMGCR_Degradation

HMGCR_Deg=compartme*delta_h*h

# Reaction: id = Cholesterol_Synthesis, name = Cholesterol_Synthesis

Cholester=compartme*Functio_2(mu_c, h)

# Reaction: id = Cholesterol_Utilisation, name = Cholesterol_Utilisation

Cholest_1=compartme*delta_c*c

# Species:   id = m, name = m, affected by kineticLaw

init m=3.65E-8
dm/dt=(1/(compartme))*(( 1.0 * HMGCR_Tra) + (-1.0 * HMGCR_mRN))

# Species:   id = h, name = h, affected by kineticLaw

init h=1.1E-5
dh/dt=(1/(compartme))*(( 1.0 * HMGCR_T_1) + (-1.0 * HMGCR_Deg))

# Species:   id = c, name = c, affected by kineticLaw

init c=0.023
dc/dt=(1/(compartme))*(( 1.0 * Cholester) + (-1.0 * Cholest_1))

@ meth=cvode, tol=1e-6, atol=1e-8
# @ maxstor=1e6
@ bound=40000, total=200
done

